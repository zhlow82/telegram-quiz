server:
  port: 8081

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

jwt:
  # Base64-encoded secret: "zhtemplate-jwt-secret-key-for-development-use-only-2026"
  # IMPORTANT: Change this in production and store it as an environment variable
  secret: emh0ZW1wbGF0ZS1qd3Qtc2VjcmV0LWtleS1mb3ItZGV2ZWxvcG1lbnQtdXNlLW9ubHktMjAyNg==
  expiration: 900000        # 15 minutes in milliseconds
  refresh-expiration: 604800000  # 7 days in milliseconds
